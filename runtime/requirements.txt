# Lab Exam Client - Bundled Python Requirements
# These packages are mandatory for student questions.
# Install into the bundled Python environment:
#   python.exe -m pip install -r requirements.txt --no-index --find-links ./wheels

pandas==2.2.2
numpy==1.26.4
matplotlib==3.8.4
seaborn==0.13.2
